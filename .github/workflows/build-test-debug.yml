name: Build & Test Debug

on:
  push:
    branches: [ master, staging, stable ]
  merge_group:
  pull_request:
    types: [ opened, reopened, synchronize, ready_for_review ]
    branches: [ master, staging, stable ]

jobs:
  build:
    if: github.actor != 'PJBot' && github.event.pull_request.draft == false
    strategy:
      matrix:
        os: [ubuntu-latest]

    runs-on: ${{ matrix.os }}

    steps:
    # KS14: Use NUnit runner directly instead of `dotnet test`
    - name: Download NUnit Console Runner # KS14 addition
      run: |
        wget https://github.com/nunit/nunit-console/releases/download/3.22.0/NUnit.Console-3.22.0.zip
        unzip NUnit.Console-3.22.0.zip -d nunit

    # KS14: Use NUnit runner directly instead of `dotnet test`
    - name: Inspect NUnit folder
      run: ls -R nunit

    - name: Checkout Master with Submodule
      uses: actions/checkout@v4.2.2
      with:
        submodules: recursive

    - name: Setup Submodule
      run: |
        git submodule update --init --recursive

    - name: Pull engine updates
      uses: space-wizards/submodule-dependency@v0.1.5

    - name: Update Engine Submodules
      run: |
        cd RobustToolbox/
        git submodule update --init --recursive

    - name: Setup .NET Core
      uses: actions/setup-dotnet@v4.1.0
      with:
        dotnet-version: 10.0.x

    - name: Install dependencies
      run: dotnet restore

    - name: Build Project # KS14: Add parallelism, remove debugging and analyzers (saves like 17.2% of time compared to previously)
      run: dotnet build --configuration DebugOpt --no-restore -p:DebugType=None -p:DebugSymbols=false -p:BuildInParallel=true -p:CL_MPCount=2 -p:RunAnalyzers=false -p:EnableNETAnalyzers=false -m

    # KS14: Use NUnit runner directly instead of `dotnet test`
    - name: Run Content.Tests (NUnit)
      run: |
        export DOTNET_gcServer=1
        ./nunit/NUnit.Console-3.22.0/bin/net6.0/nunit3-console \
          bin/Content.IntegrationTests/Content.IntegrationTests.dll \
          --workers=1 \
          --labels=Off \
          --result=none

    # KS14: Use NUnit runner directly instead of `dotnet test`
    - name: Run Content.IntegrationTests (NUnit)
      run: |
        export DOTNET_gcServer=1
        ./nunit/NUnit.Console-3.22.0/bin/net6.0/nunit3-console \
          bin/Content.IntegrationTests/Content.IntegrationTests.dll \
          --workers=1 \
          --labels=Off \
          --result=none

    # KS14: Remove test result archiving

  ci-success:
    name: Build & Test Debug
    needs:
      - build
    runs-on: ubuntu-latest
    steps:
      - name: CI succeeded
        run: exit 0
