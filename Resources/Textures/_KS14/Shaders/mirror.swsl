void fragment()
{
    highp vec2 uv = UV;

    // flip horizontally
    uv.x = 1.0 - uv.x;

    highp vec4 col = zTexture(uv);
    lowp vec4 fade = vec4(1.0, 1.0, 1.0, 1.0 - uv.y);

    COLOR = col;
}
